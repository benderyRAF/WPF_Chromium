<Window x:Class="ChromiumWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChromiumWPF"
        xmlns:cefSharp="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized">
    <Grid Background="{DynamicResource wbBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel Orientation="Horizontal">
                <TextBox x:Name="longitudeTextbox" FontSize="20" Width="300" Margin="5" VerticalAlignment="Center" ToolTip="Longitude">35.04592791446208</TextBox>
                <TextBox x:Name="latitudeTextbox" FontSize="20" Width="300" Margin="5" VerticalAlignment="Center" ToolTip="Latitude">31.48658108795938</TextBox>
                <TextBox x:Name="heightTextbox" FontSize="20" Width="100" Margin="5" VerticalAlignment="Center" ToolTip="Height">5000</TextBox>
                <Button Width="100" Height="30" Margin="10, 0, 0, 0" Background="Lime" Click="SetValues">Add point</Button>
                <Button Width="100" Height="30" Margin="10, 0, 0, 0" Background="Red" Click="ClearPoints">Clear points</Button>
            </StackPanel>
        </Grid>

        <cefSharp:ChromiumWebBrowser Name="defaultBrowser" Grid.Row="1" Address="localhost:5500/index.html" />
    </Grid>
</Window>

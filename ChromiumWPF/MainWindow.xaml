<Window x:Class="ChromiumWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChromiumWPF"
        xmlns:cefSharp="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized">
    <Grid Background="{DynamicResource wbBackground}">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="menuBarRow" Height="25" />
            <RowDefinition Height="30" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" HorizontalAlignment="Center">
            <TextBlock FontSize="30" VerticalAlignment="Center" Foreground="White">GIS</TextBlock>
        </Grid>
        
        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AutoCheckButton" Click="AutoCheckButtonClick" Width="75" Height="25" Margin="5, 0, 0, 0" Background="Lime">Auto check</Button>
                <Button x:Name="OnOffLineButton" Click="ChangeConnection" Width="25" Height="25" Margin="25, 0, 0, 0" Background="Gray"></Button>
                <TextBlock x:Name="OnOffLineText" FontSize="25" VerticalAlignment="Center" Margin="5, 0, 0, 0" Foreground="LightGreen">Online</TextBlock>
            </StackPanel>
        </Grid>
        
        <!-- Tab Control -->
        <TabControl x:Name="tabControl" Grid.Row="2" BorderThickness="0" SelectionChanged="TabControl_SelectionChanged" Padding="0"></TabControl>
    </Grid>
</Window>

<Window x:Class="ChromiumWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChromiumWPF"
        xmlns:cefSharp="clr-namespace:CefSharp.Wpf;assembly=CefSharp.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowState="Maximized">
    <Grid Background="{DynamicResource wbBackground}">
        <Grid.RowDefinitions>
            <RowDefinition x:Name="menuBarRow" Height="25" />
            <RowDefinition Height="30" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- Top Menu -->
        <Menu Height="25" VerticalAlignment="Top" Background="{DynamicResource wbSecondaryBack}" ItemContainerStyle="{StaticResource wbMenuItem}">
            <MenuItem Header="File" Foreground="{DynamicResource wbGrey}" ItemContainerStyle="{StaticResource wbSubMenuItem}">
                <MenuItem x:Name="newTabMenuItem" Header="New Tab" Click="newTabMenuItem_Click"/>
                <Separator/>
                <MenuItem x:Name="SettingsMenuItem" Header="Settings" Click="SettingsMenuItem_Click"/>
            </MenuItem>
        </Menu>
        
        <!-- AddressBar -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="300"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <!-- Nav Buttons -->
            <StackPanel Orientation="Horizontal" Height="32" Margin="10,0">
                <Button x:Name="backButton" Width="32" Margin="0,0,10,0" BorderThickness="0" Click="backButton_Click" Background="{x:Null}">
                    <Viewbox Width="32" Height="32" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image Source="/Images/Back.png"/>
                    </Viewbox>
                </Button>
                <Button x:Name="forwardButton" Width="32" Margin="0,0,10,0" BorderThickness="0" Click="forwardButton_Click" Background="{x:Null}">
                    <Viewbox Width="32" Height="32" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Image Source="/Images/Forward.png"/>
                    </Viewbox>
                </Button>
            </StackPanel>

            <Button x:Name="refreshButton" Width="32" Height="32" Margin="0,0,10,0" HorizontalAlignment="Right" BorderThickness="0" Click="refreshButton_Click" Background="{x:Null}">
                <Viewbox Width="32" Height="32" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Image Source="/Images/Refresh.png"/>
                </Viewbox>
            </Button>

            <!-- Address Bar -->
            <TextBox x:Name="AddressBar" Grid.Column="1" Height="25" VerticalAlignment="Center" VerticalContentAlignment="Center" TextWrapping="NoWrap" KeyDown="AddressBar_KeyDown"/>
        </Grid>
        
        <!-- Tab Control -->
        <TabControl x:Name="tabControl" Grid.Row="2" BorderThickness="0" SelectionChanged="tabControl_SelectionChanged" Padding="0">
            <TabItem Header="Blank page" BorderThickness="0">
                <cefSharp:ChromiumWebBrowser x:Name="defaultBrowser" Address="htttps://www.google.com" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>
